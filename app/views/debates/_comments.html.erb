<%#
  For development, hard-code the flag.
%>
<% debate_type = "survey" %>

<% if debate_type == "survey" %>
  <%# 1. RENDER THE NEW SURVEY VIEW (which we'll create next) %>
  <%= render "debates/survey_comments", debate: @debate %>
<% else %>
  <%= render Shared::CommentsComponent.new(@debate) %>
<% end %>